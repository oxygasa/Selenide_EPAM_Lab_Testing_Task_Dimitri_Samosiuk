package pages.base;

public class BasePage {
    /**
     * Here is a class for general objects for all pages.
     * @Selenide initialises and stops webdriver automatically. @Before and @After annotations doesn't need for that.
     * @Selenide always uses Implicit waits automatically by interfaces of opening and assertions.
     */
}